<%= simple_form_for :title, url: events_path, method: "GET", html: { class: 'form-inline' } do |f| %>
  <%= f.input :item, input_html: {value: ""}, label: false, placeholder: "Search for event" %>
  <%= f.submit "Search", class: "btn" %>
<% end %>
